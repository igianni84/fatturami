## Codebase Patterns
- Stack: Next.js 16 (App Router), TypeScript, Tailwind CSS 4, Prisma 6 ORM, PostgreSQL
- Use Zod for form and API validation
- UI language: Italian
- Document generation languages: ES, IT, EN
- Docker Compose for deployment (app + PostgreSQL)
- File storage: local filesystem (Docker volume)
- Single-user system (no multi-tenancy)
- Monetary amounts stored as Decimal in Prisma
- Status fields as Prisma enums
- Soft delete pattern: deletedAt timestamp field
- Progressive numbering: PREFIX-YYYY-NNN format
- VAT regime auto-detection based on client country
- Prisma client import: `import { PrismaClient } from "@prisma/client"` (Prisma 6 style, enums also imported from there)
- Seed script uses `tsx` runner: configured via `prisma.seed` field in package.json
- Seed idempotency pattern: use deterministic IDs with `upsert` (e.g., `seed-{type}-{country}`)
- Prisma singleton pattern in `src/lib/prisma.ts`
- Tailwind 4 uses `@import "tailwindcss"` in globals.css (no tailwind.config.js needed)
- PostCSS uses `@tailwindcss/postcss` plugin
- Next.js output mode: "standalone" (for Docker deployment)
- ESLint uses flat config (eslint.config.mjs) with next/core-web-vitals
- Migration generation without DB: `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script`
- Pages with DB queries need `export const dynamic = "force-dynamic"` to avoid build-time pre-rendering errors
- Server action pattern: Zod schema + safeParse validation + return `{ success, errors?, message? }`
- Form pages: server component loads data, passes to client component form via props
- Line items pattern: description, quantity, unitPrice, taxRateId (except PurchaseInvoiceLine which uses amount)
- All parent-child relations use onDelete: Cascade for line items
- Sidebar navigation component at `src/components/Sidebar.tsx` (client component with usePathname for active highlighting)
- Layout structure: flex h-screen with fixed sidebar (w-64) and scrollable main content area
- ESLint has a circular reference issue with FlatCompat + next/core-web-vitals (pre-existing, not blocking)
- Prisma singleton import: `import { prisma } from "@/lib/prisma"` (named export, NOT default)
- DataTable component at `src/components/DataTable.tsx` - reusable table with search, pagination, and actions column
- List pages pattern: server component (page.tsx) reads searchParams, calls server action, passes data to client component
- Next.js 16 searchParams is a Promise: use `const params = await searchParams` in page components
- "use server" modules: ALL exported functions must be async. Sync helpers must stay private (not exported)
- Client form pattern: client component with useRouter().push() for redirect after successful server action
- Progressive numbering pattern: findFirst with startsWith(prefix) + orderBy desc, parse and increment
- Prisma Decimal creation: `import { Decimal } from "@prisma/client/runtime/library"` then `new Decimal(value)`
- Dynamic line items: array state with updateLine(index, field, value), addLine(), removeLine(index) helpers
- Quote/Invoice form pattern: server component loads clients+taxRates, passes to client form component
- Invoice VAT logic: nazionale=standard(21%), intraUE+VAT=reverse_charge(0%), intraUE-noVAT=standard(21%), extraUE=export_exempt(0%)
- Tax rate type field used for auto-matching: standard, reduced, super_reduced, reverse_charge, export_exempt
- Invoice disclaimer auto-generation: stored on Invoice.disclaimer field, determined by client vatRegime at creation time
- Detail page pattern: server component with `notFound()` for missing records, `export const dynamic = "force-dynamic"`, grid layout for info cards
- "Create from parent" route pattern: /resource/nuova/[parentId] for child entities (e.g., credit note from invoice)
- File upload pattern: client passes File object to server action, server uses fs/promises writeFile to uploads/{module}/{timestamp}_{name}
- PurchaseInvoiceLine uses `amount` (single field) instead of quantity*unitPrice; also has `deductible` boolean flag
- API Route Handlers use route.ts (not "use server"), support FormData via `request.formData()`
- AI extraction API at /api/extract: POST with FormData file, returns ExtractionResult JSON
- Anthropic SDK: `@anthropic-ai/sdk` package, use `type: "image"` for images and `type: "document"` for PDFs in content blocks
- CSV parsing: use `papaparse` with `header: true` for column-name-keyed row objects
- Multi-step wizard pattern: `step` state variable controls render (upload → mapping → preview → importing → result)
- Sidebar navigation sections with children: `{ label, href, children: [{ label, href }] }` (see Importazioni)
- Batch processing pattern: sequential loop with status array for progress tracking; avoid Promise.all for API-rate-limited operations
- ExtractionResult from /api/extract maps to ReviewItem for editable review before import
- Charts: use `recharts` library with ResponsiveContainer (needs fixed-height parent), BarChart/LineChart components
- Fiscal report pattern: server action queries with client.vatRegime filter, client component with period selector and useEffect for data reload
- Quarter range: `start = new Date(year, (quarter-1)*3, 1)`, `end = new Date(year, (quarter-1)*3+3, 0, 23,59,59,999)`
- Fiscal section uses /fiscale/page.tsx with redirect() to default subpage (/fiscale/iva)
- PDF generation: @react-pdf/renderer with `renderToBuffer()`, wrap in `new Uint8Array()` for NextResponse, use `React.createElement()` in route handlers
- PDF template components in `src/lib/pdf/` - InvoicePDF.tsx (template), translations.ts (multi-lang labels)
- PDF API route pattern: GET /api/pdf/{type}?id=xxx&lang=ES|IT|EN, returns application/pdf with Content-Disposition header
- Reusable DownloadPDFButton at `src/components/DownloadPDFButton.tsx` - parameterized by documentId, documentType ("invoice"|"quote"|"credit-note"), defaultFilename
- Authentication: JWT (jose library) + bcryptjs password hashing, httpOnly cookie "auth-token", middleware at src/middleware.ts
- Route group pattern: `(main)` group contains all authenticated pages with sidebar layout; `/login` stays at root level without sidebar
- Cross-group imports use `@/app/(main)/...` path (route groups are transparent in URLs but NOT in file imports)
- Auth utility at `src/lib/auth.ts`: hashPassword, verifyPassword, createToken, verifyToken, setAuthCookie, removeAuthCookie, getCurrentUser
- Logout pattern: server action in `src/app/(main)/logout/actions.ts` using removeAuthCookie + redirect("/login")

---

## 2026-01-22 - US-001
- What was implemented: Next.js project initialization with TypeScript, Tailwind CSS 4, Prisma 6 ORM, Docker Compose (PostgreSQL + app), and standalone build config
- Files changed:
  - package.json, package-lock.json (dependencies)
  - tsconfig.json (TypeScript config with path aliases)
  - next.config.ts (standalone output)
  - postcss.config.mjs (Tailwind 4 PostCSS plugin)
  - eslint.config.mjs (flat ESLint config)
  - next-env.d.ts (Next.js type declarations)
  - prisma/schema.prisma (PostgreSQL datasource)
  - src/app/globals.css (Tailwind import)
  - src/app/layout.tsx (root layout)
  - src/app/page.tsx (home page)
  - src/lib/prisma.ts (Prisma singleton)
  - docker-compose.yml (PostgreSQL + app services)
  - Dockerfile (multi-stage build)
  - .gitignore, .env.example
- **Learnings for future iterations:**
  - Prisma 7.x requires a driver adapter or Accelerate URL - use Prisma 6.x for traditional PostgreSQL connection via URL
  - Tailwind 4 does not use tailwind.config.js; use `@import "tailwindcss"` in CSS and `@tailwindcss/postcss` plugin
  - Next.js `create-next-app` fails if directory has existing files - must set up manually
  - ESLint flat config requires `@eslint/eslintrc` package for compat with next configs
  - `public/` directory must exist for Docker build (COPY step)
---

## 2026-01-22 - US-002
- What was implemented: Complete Prisma schema with all core entities (Company, Client, Supplier, TaxRate, Invoice, InvoiceLine, CreditNote, CreditNoteLine, Quote, QuoteLine, PurchaseInvoice, PurchaseInvoiceLine, Expense) plus enums (VatRegime, InvoiceStatus, QuoteStatus, PurchaseInvoiceStatus, ExpenseCategory, TaxRateType)
- Files changed:
  - prisma/schema.prisma (full schema with all models, enums, relations, and indexes)
  - prisma/migrations/20260122000000_create_core_entities/migration.sql (generated SQL migration)
- **Learnings for future iterations:**
  - When Docker is not running, use `prisma migrate diff --from-empty --to-schema-datamodel` to generate migration SQL without a live DB
  - `prisma validate` and `prisma generate` work without a database connection
  - Line items (InvoiceLine, QuoteLine, CreditNoteLine) share a similar structure: description, quantity, unitPrice, taxRateId
  - PurchaseInvoiceLine uses `amount` instead of quantity/unitPrice since purchases are recorded as totals
  - All line item models use `onDelete: Cascade` to clean up when parent is deleted
  - TaxRate is referenced by all line item types via taxRateId relation
---

## 2026-01-22 - US-003
- What was implemented: Seed script for TaxRate table with Spanish IVA rates (21%, 10%, 4%) and international tax scenarios (intra-EU reverse charge 0%, extra-EU export 0%)
- Files changed:
  - prisma/seed.ts (new seed script using upsert for idempotency)
  - package.json (added `prisma.seed` config and `tsx` dev dependency)
  - package-lock.json (tsx dependency)
- **Learnings for future iterations:**
  - Prisma seed is configured via `prisma.seed` field in package.json (deprecated in Prisma 7, will need prisma.config.ts)
  - Use `tsx` to run TypeScript seed files directly
  - Use deterministic IDs with `upsert` for idempotent seeding (avoids duplicates on re-runs)
  - TaxRateType enum values: standard, reduced, super_reduced, reverse_charge, export_exempt
  - Seed script can't run without a live database (expected - `npx prisma db seed` requires Docker running)
---

## 2026-01-22 - US-004
- What was implemented: App layout with sidebar navigation including all required links (Dashboard, Fatture, Preventivi, Note Credito, Acquisti, Spese, Anagrafiche with Clienti/Fornitori sub-items, Fiscale, Impostazioni), active link highlighting using usePathname, and responsive desktop layout with fixed sidebar and scrollable content area
- Files changed:
  - src/components/Sidebar.tsx (new - client component with navigation links and active state)
  - src/app/layout.tsx (updated - flex layout with sidebar and main content area)
  - src/app/page.tsx (simplified - removed redundant padding/centering since layout handles it)
- **Learnings for future iterations:**
  - Use `"use client"` directive for components needing `usePathname` from next/navigation
  - Active link detection: exact match for "/" (dashboard), startsWith for other routes
  - Anagrafiche section uses a parent label (non-clickable) with child links indented
  - Sidebar is dark (bg-gray-800) with blue highlight for active items
  - ESLint has a circular reference issue with the FlatCompat layer for next/core-web-vitals - typecheck is the reliable quality gate
  - Next.js build with Turbopack compiles and generates static pages successfully
---

## 2026-01-22 - US-005
- What was implemented: Company settings page at /impostazioni with form for all company fields (nome, NIF/CIF, indirizzo, città, CAP, paese, email, telefono, IBAN, regime fiscale), Zod validation with NIF format check, server actions for load/save (upsert pattern), and success feedback on save
- Files changed:
  - src/app/impostazioni/page.tsx (new - server component page with dynamic rendering)
  - src/app/impostazioni/CompanyForm.tsx (new - client component form with validation display)
  - src/app/impostazioni/actions.ts (new - server actions getCompany/saveCompany with Zod schema)
  - package.json, package-lock.json (added zod dependency)
- **Learnings for future iterations:**
  - Pages that fetch from DB at render time need `export const dynamic = "force-dynamic"` to avoid static pre-rendering failures during build (when DB isn't available)
  - Server actions pattern: define Zod schema, export types, create action functions that validate then upsert
  - Form pattern: client component with useState for form data, call server action directly, display errors from returned result
  - NIF validation regex: `^(\d{8}[A-Z]|[A-Z]\d{7}[A-Z0-9])$/i` covers DNI (8 digits + letter) and NIE/CIF (letter + 7 digits + letter/digit)
  - Single company record pattern: findFirst to check existence, then update or create accordingly
---

## 2026-01-22 - US-006
- What was implemented: Client list page at /anagrafiche/clienti with reusable DataTable component, search by name/VAT number, pagination (10 per page), "Nuovo Cliente" button, and server action excluding soft-deleted clients
- Files changed:
  - src/components/DataTable.tsx (new - reusable generic table with search, pagination, actions column)
  - src/app/anagrafiche/clienti/page.tsx (new - server component with dynamic rendering, reads searchParams)
  - src/app/anagrafiche/clienti/ClientList.tsx (new - client component for interactivity, uses DataTable)
  - src/app/anagrafiche/clienti/actions.ts (new - getClients server action with search/pagination)
- **Learnings for future iterations:**
  - Prisma singleton uses named export: `import { prisma } from "@/lib/prisma"` (not default export)
  - Next.js 16 searchParams in page components is a Promise that must be awaited
  - DataTable component is generic (`<T extends { id: string }>`) - items must have an id field
  - Search uses Prisma `contains` with `mode: "insensitive"` for case-insensitive matching
  - List page pattern: server component reads URL params → calls server action → passes data to client component
  - Client component uses `useRouter` + `useSearchParams` to update URL on search/pagination changes
---

## 2026-01-22 - US-007
- What was implemented: Client create/edit form with Zod validation, VAT regime auto-detection by country, VAT number format validation per country, country dropdown with EU/Extra-EU grouping, and redirect to client list after save
- Files changed:
  - src/app/anagrafiche/clienti/actions.ts (added createClient, updateClient, getClient actions with Zod schema, VAT validation, and regime detection)
  - src/app/anagrafiche/clienti/ClientForm.tsx (new - client form component with country selection and regime display)
  - src/app/anagrafiche/clienti/nuovo/page.tsx (new - create client page)
  - src/app/anagrafiche/clienti/[id]/modifica/page.tsx (new - edit client page with dynamic rendering)
- **Learnings for future iterations:**
  - In "use server" modules, ALL exported functions must be async (server actions). Pure sync helper functions must NOT be exported - keep them private or move to a shared utility module
  - VAT regime detection logic: ES=nazionale, EU countries=intraUE, everything else=extraUE
  - Client form uses `useRouter().push()` for redirect after successful save
  - Edit page uses `export const dynamic = "force-dynamic"` since it fetches from DB at render time
  - Next.js 16 dynamic route params are a Promise: `params: Promise<{ id: string }>` must be awaited
  - Country dropdown with optgroup for EU/Extra-EU separation improves UX
---

## 2026-01-22 - US-008
- What was implemented: Soft delete functionality for clients - delete button on client list rows, confirmation dialog, server action setting deletedAt timestamp, and list auto-refreshes after deletion
- Files changed:
  - src/app/anagrafiche/clienti/actions.ts (added deleteClient server action)
  - src/app/anagrafiche/clienti/ClientList.tsx (added delete button with confirmation modal, edit link in actions column)
- **Learnings for future iterations:**
  - DataTable `actions` prop accepts a render function `(item: T) => React.ReactNode` for per-row action buttons
  - Confirmation dialog pattern: use useState with the item to delete, modal overlay with confirm/cancel buttons
  - After server action mutation, use `router.refresh()` to refetch server component data without full page reload
  - Soft delete is simply `prisma.client.update({ data: { deletedAt: new Date() } })` - getClients already filters by `deletedAt: null`
---

## 2026-01-22 - US-009
- What was implemented: Supplier list page at /anagrafiche/fornitori with search/pagination, create form at /anagrafiche/fornitori/nuovo, edit form at /anagrafiche/fornitori/[id]/modifica, soft-delete with confirmation dialog, and expense category dropdown (servizi professionali, software, hardware, viaggi, telecomunicazioni, altro)
- Files changed:
  - src/app/anagrafiche/fornitori/actions.ts (server actions: getSuppliers, getSupplier, createSupplier, updateSupplier, deleteSupplier)
  - src/app/anagrafiche/fornitori/SupplierList.tsx (client component with DataTable, delete modal, category labels)
  - src/app/anagrafiche/fornitori/page.tsx (server component with dynamic rendering)
  - src/app/anagrafiche/fornitori/SupplierForm.tsx (client form with country and expense category dropdowns)
  - src/app/anagrafiche/fornitori/nuovo/page.tsx (create page)
  - src/app/anagrafiche/fornitori/[id]/modifica/page.tsx (edit page with dynamic rendering)
- **Learnings for future iterations:**
  - DataTable `render` function receives the full item, not just the cell value: `render: (item) => ...`
  - Supplier model uses `ExpenseCategory` enum from Prisma (same enum used by PurchaseInvoice and Expense models)
  - Zod validation for enum fields: use `z.string().refine((val) => VALID_VALUES.includes(val))` since Zod doesn't natively validate Prisma enums
  - When passing Prisma enum values from Zod-validated string, cast with `as ExpenseCategory` in the create/update data
  - Supplier form is simpler than Client form: no VAT regime detection, no fiscal code, no currency preference
---

## 2026-01-22 - US-010
- What was implemented: Quote creation form at /preventivi/nuovo with client selection dropdown, date/expiry date fields, dynamic line items (description, quantity, unit price, tax rate), automatic calculation of subtotals/tax/total per line and overall, progressive numbering PREV-YYYY-NNN, and save with status 'bozza'
- Files changed:
  - src/app/preventivi/actions.ts (new - server actions: getClientsForSelect, getTaxRates, createQuote with Zod validation and progressive numbering)
  - src/app/preventivi/QuoteForm.tsx (new - client component form with dynamic line items and live totals)
  - src/app/preventivi/nuovo/page.tsx (new - server component page loading clients and tax rates)
  - src/app/preventivi/page.tsx (new - placeholder list page with "Nuovo Preventivo" button)
  - prd.json (US-010 passes: true)
- **Learnings for future iterations:**
  - Progressive numbering pattern: query last record with `startsWith(prefix)`, parse last segment, increment
  - Dynamic line items pattern: array of objects in state, updateLine/addLine/removeLine helpers
  - Live totals: computed from state on each render (no separate state needed)
  - Prisma Decimal import: `import { Decimal } from "@prisma/client/runtime/library"` for creating Decimal values
  - Quote form reuses tax rates from TaxRate table via getTaxRates() server action
  - Placeholder list page needed for redirect target (will be fully implemented in US-011)
---

## 2026-01-22 - US-011
- What was implemented: Quote list page at /preventivi showing all quotes with DataTable (columns: number, client, date, expiry, total, status), colored status badges (bozza/inviato/accettato/rifiutato/scaduto), status change dropdown actions with valid transitions, edit link only for bozza quotes, and pagination
- Files changed:
  - src/app/preventivi/actions.ts (added getQuotes with pagination/totals calculation, updateQuoteStatus with valid transition enforcement)
  - src/app/preventivi/QuoteList.tsx (new - client component with DataTable, status badges, status change dropdown menu)
  - src/app/preventivi/page.tsx (updated - server component fetching quotes and passing to QuoteList)
  - prd.json (US-011 passes: true)
- **Learnings for future iterations:**
  - Quote total is computed from lines (sum of quantity*unitPrice + tax per line) since it's not stored on the Quote model
  - Status transitions pattern: define a mapping of current status -> valid next statuses, render only valid transitions
  - Status badge pattern: use tailwind `rounded-full` with color classes per status value
  - Dropdown menu pattern: use relative positioning on button container, absolute on the menu with z-index
  - Quote status enum includes "convertito" (used when quote is converted to invoice in US-015) but should not be a user-selectable transition
---

## 2026-01-22 - US-012
- What was implemented: Invoice creation form at /fatture/nuova with client selection (showing country), automatic VAT regime detection and tax rate auto-selection, date/due date fields, currency selection (EUR/GBP/USD) with exchange rate field for non-EUR, dynamic line items with auto-calculated totals, progressive numbering INV-YYYY-NNN, legal disclaimer auto-generation based on VAT regime, and save with status 'bozza'
- Files changed:
  - src/app/fatture/actions.ts (new - server actions: getClientsForInvoice with VAT info, getTaxRatesForInvoice with type, createInvoice with VAT logic and disclaimer generation, getDefaultTaxRateForClient)
  - src/app/fatture/InvoiceForm.tsx (new - client component form with dynamic lines, VAT auto-selection on client change, currency/exchange rate support)
  - src/app/fatture/nuova/page.tsx (new - server component page loading clients and tax rates)
  - src/app/fatture/page.tsx (new - placeholder list page with "Nuova Fattura" button)
  - prd.json (US-012 passes: true)
- **Learnings for future iterations:**
  - Invoice form extends quote form pattern with: currency selection, exchange rate, VAT regime display, and auto tax rate selection
  - VAT regime logic: nazionale=standard(21%), intraUE+validVAT=reverse_charge(0%), intraUE+noVAT=standard(21%), extraUE=export_exempt(0%)
  - ClientOptionWithVat includes vatRegime, vatNumber, currency, country - more data than simple ClientOption
  - Tax rates have a `type` field (standard, reverse_charge, export_exempt) used for auto-matching by VAT regime
  - Disclaimer is generated server-side in createInvoice based on client's vatRegime and stored on the invoice
  - Currency symbol display uses a simple ternary: EUR=€, GBP=£, else=$
  - Exchange rate field only shown when currency is not EUR (conditional rendering)
  - useEffect with clientId dependency handles auto-setting currency and tax rates when client changes
---

## 2026-01-22 - US-013
- What was implemented: Invoice list page at /fatture showing all invoices with DataTable (columns: number, client, date, due date, total, currency, status), colored status badges (bozza/emessa/inviata/pagata/scaduta), status change dropdown with valid transitions, status filter buttons, edit link for bozza invoices, detail link, and pagination
- Files changed:
  - src/app/fatture/actions.ts (added getInvoices with pagination/status filter/total calculation, updateInvoiceStatus with valid status enforcement, imported InvoiceStatus and Prisma types)
  - src/app/fatture/InvoiceList.tsx (new - client component with DataTable, status badges, filter tabs, status change dropdown)
  - src/app/fatture/page.tsx (updated - server component fetching invoices with searchParams and passing to InvoiceList)
  - prd.json (US-013 passes: true)
- **Learnings for future iterations:**
  - Invoice total is computed from lines (sum of quantity*unitPrice + tax per line) since it's not stored on the Invoice model (same pattern as quotes)
  - Prisma where clause typing: use `Prisma.InvoiceWhereInput` type and cast status string to `InvoiceStatus` enum for type safety
  - Status filter pattern: URL param "status" with "tutti" as default/clear-all; filter buttons update URL and reset to page 1
  - Invoice status transitions: bozza→emessa, emessa→inviata/pagata/scaduta, inviata→pagata/scaduta, scaduta→pagata
  - formatCurrency helper shows currency-specific symbol based on the invoice's currency field
---

## 2026-01-22 - US-014
- What was implemented: Invoice legal disclaimers based on VAT regime - updated generateDisclaimer to include standard Spanish disclaimer for domestic invoices ("Factura sujeta a IVA conforme al artículo 164 de la Ley 37/1992"), reverse charge for intra-EU with valid VAT, export exempt for extra-EU. Created invoice detail page at /fatture/[id] showing full invoice info (client details, line items, totals, disclaimer, and notes).
- Files changed:
  - src/app/fatture/actions.ts (updated generateDisclaimer to cover all regimes, added getInvoice server action with InvoiceDetail type)
  - src/app/fatture/[id]/page.tsx (new - invoice detail page with all fields, line items table, totals, disclaimer display, and notes)
- **Learnings for future iterations:**
  - Invoice detail page pattern: server component with notFound() for missing records, grid layout for info cards, table for line items, conditional disclaimer/notes sections
  - Disclaimer display uses a yellow-tinted box (bg-yellow-50, border-yellow-200) to draw attention to legal text
  - VatRegime.nazionale needs a standard Spanish tax disclaimer (not empty string) - "Factura sujeta a IVA conforme al artículo 164 de la Ley 37/1992"
  - IntraUE without valid VAT should also use the standard Spanish disclaimer (since 21% IVA applies)
  - Detail pages reuse statusLabels/statusColors patterns from list pages
---

## 2026-01-22 - US-015
- What was implemented: Convert quote to invoice functionality - "Converti in fattura" button visible only on quotes with status "accettato", creates a new invoice pre-filled with quote data (client, lines, notes), generates next progressive invoice number, updates quote status to "convertito", and redirects to the new invoice detail page
- Files changed:
  - src/app/fatture/actions.ts (added convertQuoteToInvoice server action)
  - src/app/preventivi/QuoteList.tsx (added convert button for accettato quotes, import of convertQuoteToInvoice, convertito status label/color, loading state during conversion)
  - prd.json (US-015 passes: true)
- **Learnings for future iterations:**
  - Quote-to-invoice conversion reuses existing generateInvoiceNumber and generateDisclaimer helpers from fatture/actions.ts
  - QuoteLine and InvoiceLine share the same structure (description, quantity, unitPrice, taxRateId) so lines can be copied directly
  - The "convertito" status was already defined in the QuoteStatus enum in schema.prisma but not shown in the UI - added to statusLabels/statusColors
  - Cross-module server action import works fine: client component imports from "@/app/fatture/actions" while being in preventivi
  - Conversion sets invoice date to current date (new Date()) and currency from client's preferred currency
---

## 2026-01-22 - US-016
- What was implemented: Credit note creation from invoice - "Crea nota di credito" button on invoice detail page (only for emessa/inviata/pagata statuses), credit note form pre-filled with invoice lines allowing partial credit (remove/adjust lines), progressive numbering NC-YYYY-NNN, reference to original invoice visible, detail page for credit notes, and placeholder list page at /note-credito
- Files changed:
  - src/app/note-credito/actions.ts (new - server actions: getInvoiceForCreditNote, createCreditNote with Zod validation and progressive numbering)
  - src/app/note-credito/nuova/[invoiceId]/page.tsx (new - server component loading invoice data for credit note form)
  - src/app/note-credito/nuova/[invoiceId]/CreditNoteForm.tsx (new - client component form with pre-filled invoice lines, remove/adjust support)
  - src/app/note-credito/[id]/page.tsx (new - credit note detail page with line items, totals, reference to original invoice)
  - src/app/note-credito/page.tsx (new - placeholder list page for navigation)
  - src/app/fatture/[id]/page.tsx (updated - added "Crea nota di credito" button for emessa/inviata/pagata invoices)
  - prd.json (US-016 passes: true)
- **Learnings for future iterations:**
  - Credit note form is pre-filled from invoice lines but allows removing lines for partial credit (no add line needed since credit can only reduce)
  - Credit note uses same VAT logic as original invoice (same taxRateId on lines)
  - Route pattern for "create from parent": /note-credito/nuova/[invoiceId] passes parent ID as route param
  - CreditNote model has invoiceId relation; currency is inherited from the parent invoice (not stored on credit note)
  - Credit note detail page reads currency from the related invoice record
  - Button visibility condition uses multiple status OR checks: `invoice.status === "emessa" || invoice.status === "inviata" || invoice.status === "pagata"`
---

## 2026-01-22 - US-017
- What was implemented: Credit note list page at /note-credito showing all credit notes with DataTable (columns: number, related invoice with link, client, date, total), link to view detail page, and pagination
- Files changed:
  - src/app/note-credito/actions.ts (added getCreditNotes server action with CreditNoteListItem type, pagination, and total calculation)
  - src/app/note-credito/CreditNoteList.tsx (new - client component with DataTable, invoice link, currency formatting, detail link)
  - src/app/note-credito/page.tsx (updated - server component reading searchParams and calling getCreditNotes)
  - prd.json (US-017 passes: true)
- **Learnings for future iterations:**
  - Credit note total is computed from lines (same pattern as invoice/quote totals) since it's not stored on the model
  - Credit note currency comes from the related invoice (cn.invoice.currency) - not stored on the credit note itself
  - Credit note list is simpler than invoice/quote lists - no status management needed (credit notes don't have status changes)
  - Related invoice column uses a Link component to navigate to the parent invoice detail page
---

## 2026-01-22 - US-018
- What was implemented: Purchase invoice registration form at /acquisti/nuovo with supplier selection (auto-sets expense category), supplier invoice number, date, expense category dropdown, file upload for PDF/image, dynamic line items (description, amount, tax rate, deductible flag), automatic totals calculation, and save with status 'registrata'. Also created placeholder list page at /acquisti.
- Files changed:
  - src/app/acquisti/actions.ts (new - server actions: getSuppliersForSelect, getTaxRates, createPurchaseInvoice with file upload support)
  - src/app/acquisti/nuovo/PurchaseInvoiceForm.tsx (new - client component form with dynamic lines, file upload, deductible flags)
  - src/app/acquisti/nuovo/page.tsx (new - server component loading suppliers and tax rates)
  - src/app/acquisti/page.tsx (new - placeholder list page with "Nuova fattura acquisto" button)
- **Learnings for future iterations:**
  - File upload pattern: use native File API in client component, pass File object to server action, write with fs/promises to uploads/ directory
  - PurchaseInvoiceLine uses `amount` (single field) instead of quantity*unitPrice (different from InvoiceLine/QuoteLine)
  - PurchaseInvoiceLine has a `deductible` boolean flag for VAT deductibility per line
  - Supplier dropdown can include expenseCategory to auto-populate the category field when supplier is selected
  - Upload directory structure: uploads/acquisti/{timestamp}_{sanitized_filename} for purchase invoice attachments
  - The `mkdir` with `recursive: true` ensures upload directory exists before writing
---

## 2026-01-22 - US-019
- What was implemented: Purchase invoice list page at /acquisti showing all purchase invoices with DataTable (columns: supplier, number, date, category, total, status), colored status badges (registrata/pagata), filters by supplier dropdown, date range (from/to date inputs), category dropdown, and status buttons, action to mark as 'pagata', and pagination
- Files changed:
  - src/app/acquisti/actions.ts (added getPurchaseInvoices with filters/pagination/total calculation, updatePurchaseInvoiceStatus, imported Prisma types)
  - src/app/acquisti/PurchaseInvoiceList.tsx (new - client component with DataTable, filters, status badges, mark-paid action)
  - src/app/acquisti/page.tsx (updated - server component fetching purchase invoices and suppliers with searchParams)
  - prd.json (US-019 passes: true)
- **Learnings for future iterations:**
  - Purchase invoice total is computed from lines: amount + (amount * taxRate/100) per line (uses amount field, not quantity*unitPrice)
  - PurchaseInvoiceStatus only has two values: registrata and pagata (simpler than InvoiceStatus)
  - Filter pattern: URL search params for all filters, navigateTo helper that updates URLSearchParams and pushes to router
  - Category filter uses "tutte" as the "all" value (feminine form for "categorie"), status uses "tutti"
  - Date range filter uses native HTML date inputs with onChange triggering navigation
  - Supplier filter reuses getSuppliersForSelect() already defined for the create form
---

## 2026-01-22 - US-020
- What was implemented: Expense registration form at /spese/nuova with date, description, amount, category dropdown (trasporti, pasti, materiale ufficio, telecomunicazioni, altro), tax amount field, deducibile flag, file upload for receipt PDF/image, totals summary, and server action to save expense. Also created placeholder list page at /spese.
- Files changed:
  - src/app/spese/actions.ts (new - server actions: createExpense with Zod validation and file upload)
  - src/app/spese/nuova/ExpenseForm.tsx (new - client component form with all fields)
  - src/app/spese/nuova/page.tsx (new - server component page)
  - src/app/spese/page.tsx (new - placeholder list page with "Nuova Spesa" button)
- **Learnings for future iterations:**
  - Expense model is simpler than PurchaseInvoice: no line items, single amount + taxAmount fields
  - Expense categories for the form (trasporti, pasti, materiale_ufficio, telecomunicazioni, altro) are a subset of the full ExpenseCategory enum
  - Upload directory for expenses: uploads/spese/{timestamp}_{sanitized_filename}
  - ExpenseForm doesn't need data loaded from server (no dropdowns requiring DB data), so page.tsx is minimal
  - The taxAmount field is separate from amount (not computed from a rate) - user enters the actual IVA amount from the receipt
---

## 2026-01-22 - US-021
- What was implemented: Expense list page at /spese showing all expenses with DataTable (columns: date, description, category, amount, deductible flag badge), filters (date range, category dropdown, deductible/all buttons), total amount displayed for filtered results, and pagination
- Files changed:
  - src/app/spese/actions.ts (added getExpenses server action with filters, pagination, aggregate total, ExpenseListItem/ExpenseListResult types)
  - src/app/spese/ExpenseList.tsx (new - client component with DataTable, filters, total display)
  - src/app/spese/page.tsx (updated - server component reading searchParams and calling getExpenses)
- **Learnings for future iterations:**
  - Prisma aggregate `_sum` returns Decimal|null - use `Number(result._sum.field || 0)` for safe conversion
  - Expense list is simpler than PurchaseInvoice list: no status management, no supplier filter, but adds deductible filter
  - Deductible filter uses "si"/"no" string values in URL params, mapped to boolean in the where clause
  - Total amount display uses aggregate query matching the same `where` clause as the list query for consistency
  - Category labels map covers all ExpenseCategory enum values (not just the subset used in the expense form)
---

## 2026-01-22 - US-022
- What was implemented: AI data extraction API route at /api/extract that accepts file uploads (PDF, JPG, PNG, WebP, GIF), sends them to Claude API with vision capabilities, extracts structured invoice/receipt data (supplier name, VAT number, invoice number, date, line items, amounts, tax, total), and matches extracted supplier VAT numbers against existing suppliers in the database
- Files changed:
  - src/app/api/extract/route.ts (new - POST API route with file upload, Claude Vision extraction, supplier matching)
  - package.json, package-lock.json (added @anthropic-ai/sdk dependency)
  - .env.example (added ANTHROPIC_API_KEY variable)
  - prd.json (US-022 passes: true)
- **Learnings for future iterations:**
  - API Route Handlers (route.ts) do NOT need "use server" directive - they are server-side by default
  - Anthropic SDK uses `type: "image"` with `source: { type: "base64", media_type, data }` for image content blocks
  - For PDFs, use `type: "document"` instead of `type: "image"` in the content block
  - Claude Vision model: use "claude-3-5-sonnet-latest" for extraction tasks
  - JSON parsing from AI responses: always try direct parse first, then fallback to regex extraction from markdown code blocks
  - Supplier matching by VAT: normalize (remove spaces/dots/hyphens, uppercase) then use Prisma `contains` with `mode: "insensitive"`
  - ExtractionResult type exported from the route for use by consumer components (US-023)
  - Next.js API routes support FormData natively via `request.formData()`
---

## 2026-01-22 - US-023
- What was implemented: AI extraction integration in purchase invoice and expense forms - when a file is uploaded, the /api/extract endpoint is called to extract data using Claude Vision, form fields are auto-filled with extracted data, auto-filled fields are highlighted with a blue ring, matched suppliers are auto-selected in dropdown, unmatched suppliers show a suggestion to create new, and users can manually correct any field before saving
- Files changed:
  - src/app/acquisti/nuovo/PurchaseInvoiceForm.tsx (added AI extraction on file upload, auto-fill for supplier/number/date/category/lines, field highlighting, unmatched supplier warning with link to create new)
  - src/app/spese/nuova/ExpenseForm.tsx (added AI extraction on file upload, auto-fill for date/description/amount/taxAmount, field highlighting)
  - prd.json (US-023 passes: true)
- **Learnings for future iterations:**
  - Import `type` from route.ts works for sharing ExtractionResult interface between API route and client components: `import type { ExtractionResult } from "@/app/api/extract/route"`
  - AI extraction integration pattern: handleFileChange triggers fetch to /api/extract, then maps result fields to form state, tracks auto-filled fields in a Set for highlighting
  - Field highlighting pattern: `ring-2 ring-blue-300 bg-blue-50` classes conditionally applied based on Set membership
  - For tax rate matching in purchase invoice lines, use approximate match: `Math.abs(r.rate - item.taxRate) < 0.5` to handle float precision
  - Expense form maps AI result differently: supplierName → description, subtotal → amount, taxAmount → taxAmount (no line items)
  - Unmatched supplier pattern: show amber warning with supplier name and link to /anagrafiche/fornitori/nuovo for creating a new supplier
  - Loading state during extraction: disable file input + show spinner animation with `animate-spin` on a border-based circle
---

## 2026-01-22 - US-024
- What was implemented: CSV import page at /import/csv with file upload, column mapping interface, preview of first 5 rows, data validation (dates, required fields, numeric amounts), import of issued invoices (creating Invoice + InvoiceLine records) and received invoices (creating PurchaseInvoice + PurchaseInvoiceLine records), auto-creation of clients/suppliers if not found, import report with counts and errors, toggle between issued/received invoice types, and duplicate detection
- Files changed:
  - src/app/import/csv/page.tsx (new - server component page with dynamic rendering)
  - src/app/import/csv/CSVImporter.tsx (new - client component with multi-step flow: upload, mapping, preview, import, results)
  - src/app/import/csv/actions.ts (new - server action importCSV with validation, client/supplier creation, invoice/purchase creation)
  - src/components/Sidebar.tsx (updated - added Importazioni section with CSV and PDF links)
  - package.json, package-lock.json (added papaparse + @types/papaparse)
- **Learnings for future iterations:**
  - PapaParser handles CSV parsing with `header: true` option which returns data as array of objects keyed by column names
  - Multi-step wizard pattern: use a `step` state variable to control which section renders (upload → mapping → preview → importing → result)
  - Column mapping pattern: store mapping as `{ systemField: csvColumnName }` object, auto-detect by comparing field labels/keys with CSV headers
  - For issued invoices, group CSV rows by invoice number to create one Invoice with multiple InvoiceLine records
  - For received invoices, group by supplier+invoiceNumber to create one PurchaseInvoice with multiple PurchaseInvoiceLine records
  - Date parsing supports multiple formats: ISO (YYYY-MM-DD), EU (DD/MM/YYYY, DD-MM-YYYY, DD.MM.YYYY), and fallback to native Date
  - Number parsing handles comma as decimal separator (common in EU CSV exports)
  - Auto-creation of clients/suppliers uses a Map for deduplication within the import batch
  - Duplicate invoice detection: check `prisma.invoice.findUnique({ where: { number } })` before creating
  - Import type toggle (issued/received) resets the entire wizard state when changed
---

## 2026-01-22 - US-025
- What was implemented: Batch PDF import page at /import/pdf with multi-file upload (PDF, JPG, PNG, WebP, GIF), sequential AI extraction processing with progress bar, review page showing all extracted data with expandable edit forms per file, editable line items (description, amount, tax rate, deductible flag), supplier/invoice number/date/category editing, confirm button to import all reviewed items as purchase invoices, auto-creation of new suppliers, and detailed result report
- Files changed:
  - src/app/import/pdf/page.tsx (new - server component page with dynamic rendering)
  - src/app/import/pdf/PDFBatchImporter.tsx (new - client component with multi-step flow: upload, processing, review, importing, result)
  - src/app/import/pdf/actions.ts (new - server action importPDFBatch with supplier creation, duplicate detection, purchase invoice creation)
  - prd.json (US-025 passes: true)
- **Learnings for future iterations:**
  - Sequential file processing pattern: use for loop with await inside (not Promise.all) to avoid API rate limits
  - Progress tracking pattern: array of FileProcessingStatus objects updated via spread copy on each iteration for React re-render
  - Review item pattern: ExtractionResult mapped to editable ReviewItem with additional fields (category, deductible per line)
  - Expandable card pattern: editingIndex state controls which item shows the edit form; click header to toggle
  - When no lines are extracted from AI, create a fallback line from subtotal/total in the import action
  - Duplicate detection for PDF imports: check by supplierId + invoiceNumber before creating
  - File validation on client side: check file.type against allowedTypes array before adding to selection
  - Multi-file input: use `multiple` attribute on input[type=file] for batch selection
---

## 2026-01-22 - US-026
- What was implemented: Dashboard page at / (home) with period selector (month/quarter/year), four metric cards (revenue from issued invoices, expenses from purchases+expenses, balance, overdue invoice count), and a recent documents list showing last 5 invoices and purchases sorted by date
- Files changed:
  - src/app/dashboard/actions.ts (new - getDashboardData server action with period range calculation, revenue/expense aggregation, overdue count, recent docs)
  - src/app/dashboard/Dashboard.tsx (new - client component with period selector, metric cards, recent documents list)
  - src/app/page.tsx (updated - server component loading initial data for month period)
  - prd.json (US-026 passes: true)
- **Learnings for future iterations:**
  - Dashboard pattern: server component loads initial data for default period, client component handles period switching via useEffect
  - Revenue calculation: sum of (quantity * unitPrice + tax) for all lines of invoices with status emessa/inviata/pagata in the period
  - Expense calculation: purchase invoice line totals (amount + tax) + expense aggregate (amount + taxAmount)
  - Overdue invoices: count where status is emessa/inviata AND dueDate < now
  - Period range helper: month uses current month, quarter uses floor(month/3)*3, year uses Jan 1 to Dec 31
  - Recent documents combine invoices and purchases, sorted by date desc, limited to 5
  - Loading state: use opacity-60 on the cards grid during data fetch for visual feedback
---

## 2026-01-22 - US-027
- What was implemented: Monthly revenue bar chart on the dashboard using recharts library, showing revenue per month for the current year. Server action fetches all issued invoices for the year and groups revenue by month. Chart uses ResponsiveContainer for responsive sizing, BarChart with CartesianGrid, XAxis (month abbreviations), YAxis (formatted with € and k suffix), and Tooltip with formatted currency values.
- Files changed:
  - src/app/dashboard/actions.ts (added MonthlyRevenue type, monthlyRevenue calculation for current year, included in DashboardData response)
  - src/app/dashboard/Dashboard.tsx (added recharts imports, revenue bar chart section between metrics cards and recent documents)
  - package.json, package-lock.json (added recharts dependency)
  - prd.json (US-027 passes: true)
- **Learnings for future iterations:**
  - recharts Tooltip `formatter` parameter type is `value: number | undefined` - use `Number(value)` instead of typing as number
  - recharts ResponsiveContainer needs explicit width="100%" and height="100%" with a fixed-height parent container
  - Bar chart with `radius={[4, 4, 0, 0]}` gives rounded top corners for a modern look
  - YAxis tickFormatter can abbreviate large values: `value >= 1000 ? (value/1000).toFixed(0) + 'k' : value`
  - Monthly revenue query reuses the same invoice total calculation pattern (quantity*unitPrice + tax per line)
  - Chart data is always 12 items (one per month) with 0 revenue for months without invoices
---

## 2026-01-22 - US-028
- What was implemented: Quarterly VAT report page at /fiscale/iva with quarter/year selector, IVA Repercutida (VAT on domestic sales only), IVA Soportada Deducible (deductible VAT on purchases), IVA result (to pay/credit), breakdown by tax rate table, and intra-EU operations summary (for Modelo 349). Updated Sidebar with Fiscale children links (IVA, IRPF). Created /fiscale redirect page.
- Files changed:
  - src/app/fiscale/iva/actions.ts (new - getVATReport server action with domestic sales VAT, deductible purchase VAT, intra-EU operations)
  - src/app/fiscale/iva/VATReport.tsx (new - client component with period selector, summary cards, rate breakdown table, intra-EU table)
  - src/app/fiscale/iva/page.tsx (new - server component loading initial data for current quarter)
  - src/app/fiscale/page.tsx (new - redirect to /fiscale/iva)
  - src/components/Sidebar.tsx (updated - Fiscale now has children: IVA, IRPF)
  - prd.json (US-028 passes: true)
- **Learnings for future iterations:**
  - VAT report query pattern: filter invoices by client.vatRegime to separate domestic from intra-EU/extra-EU
  - IVA Repercutida only counts invoices where client.vatRegime === "nazionale" (domestic Spanish)
  - IVA Soportada only counts purchase invoice lines where deductible === true
  - Quarter range calculation: `(quarter - 1) * 3` for start month, `(quarter - 1) * 3 + 3` for end month boundary
  - Rate breakdown pattern: Map keyed by taxRate.id, accumulating sales and purchase amounts per rate
  - Intra-EU operations summary: separate query filtering client.vatRegime === "intraUE", shows base amounts (no tax since reverse charge)
  - Sidebar children pattern works for Fiscale section same as Anagrafiche/Importazioni
  - /fiscale/page.tsx uses `redirect()` from next/navigation to default to /fiscale/iva
---

## 2026-01-22 - US-029
- What was implemented: IRPF income estimate page at /fiscale/irpf (Modelo 130) with year selector, quarterly breakdown table (revenue, deductible expenses, net income, pago fraccionado at 20%), annual summary cards (net income, estimated IRPF, total pagos fraccionados), IRPF bracket breakdown table using Spanish 2024 tax brackets (19/24/30/37/45%), withholdings section, and informational note about consulting a tax advisor
- Files changed:
  - src/app/fiscale/irpf/actions.ts (new - getIRPFReport server action with quarterly revenue/expense aggregation, IRPF bracket calculation, pago fraccionado computation)
  - src/app/fiscale/irpf/IRPFReport.tsx (new - client component with year selector, quarterly table, summary cards, bracket table)
  - src/app/fiscale/irpf/page.tsx (new - server component loading initial data for current year)
  - prd.json (US-029 passes: true)
- **Learnings for future iterations:**
  - IRPF report is year-based (not quarterly) since brackets apply to annual income, but shows quarterly pagos fraccionados
  - Pago fraccionado (Modelo 130) = 20% of quarterly net income (revenue - deductible expenses), floored at 0
  - IRPF bracket calculation: iterate brackets, subtract bracket width from remaining income, compute tax at bracket rate
  - Revenue for IRPF uses base amounts (quantity * unitPrice) without VAT - IRPF is on income, not on VAT collected
  - All purchase invoice line amounts count as expenses for IRPF (not just deductible ones for VAT purposes)
  - Regular expenses only count if deductible === true
  - IRPF page follows same pattern as IVA page but uses year selector instead of quarter+year
---

## 2026-01-22 - US-030
- What was implemented: PDF generation for invoices using @react-pdf/renderer. Professional template with company logo placeholder, company data, client info, invoice details, line items table, totals, legal disclaimers, bank details (IBAN), and notes. Multi-language support (ES, IT, EN) via language selector dropdown on the download button. API route at /api/pdf/invoice with GET params (id, lang).
- Files changed:
  - package.json, package-lock.json (added @react-pdf/renderer)
  - src/lib/pdf/translations.ts (new - multi-language translation map for PDF labels)
  - src/lib/pdf/InvoicePDF.tsx (new - React PDF document component with styled template)
  - src/app/api/pdf/invoice/route.ts (new - GET API route that fetches invoice+company data and renders PDF)
  - src/app/fatture/[id]/DownloadPDFButton.tsx (new - client component with language selector and download trigger)
  - src/app/fatture/[id]/page.tsx (updated - added DownloadPDFButton to action bar)
  - prd.json (US-030 passes: true)
- **Learnings for future iterations:**
  - @react-pdf/renderer uses `renderToBuffer()` for server-side PDF generation; the Buffer must be wrapped in `new Uint8Array()` for NextResponse body
  - PDF template uses `React.createElement()` in route.ts (no JSX in non-component files) to render the document component
  - @react-pdf/renderer StyleSheet.create() uses a CSS-like object syntax but with camelCase and limited CSS properties
  - Font families available without registration: Helvetica, Helvetica-Bold, Courier, Times-Roman
  - PDF template supports three document types (invoice, quote, creditNote) via the `type` field on PDFInvoiceData - reusable for US-031
  - The PDFInvoiceData interface is generic enough to handle quotes (expiryDate) and credit notes (referenceInvoice)
  - Download button pattern: fetch blob from API route, create object URL, trigger programmatic click on anchor element, then cleanup
  - Company data may be null (not configured yet) - provide empty fallback object
---

## 2026-01-22 - US-031
- What was implemented: PDF generation for quotes and credit notes reusing the InvoicePDF template component from US-030. Created API routes at /api/pdf/quote and /api/pdf/credit-note. Created a quote detail page at /preventivi/[id] with download button. Added download button to credit note detail page. Created a shared DownloadPDFButton component. Added "Dettagli" link to quote list for navigating to the detail page.
- Files changed:
  - src/app/api/pdf/quote/route.ts (new - GET route fetching quote+company, rendering PDF with type "quote")
  - src/app/api/pdf/credit-note/route.ts (new - GET route fetching credit note+invoice+company, rendering PDF with type "creditNote" and referenceInvoice)
  - src/components/DownloadPDFButton.tsx (new - reusable client component with language selector and download trigger, parameterized by documentType)
  - src/app/preventivi/[id]/page.tsx (new - quote detail page with line items, totals, status badge, and download button)
  - src/app/note-credito/[id]/page.tsx (updated - added DownloadPDFButton import and component in header)
  - src/app/preventivi/QuoteList.tsx (updated - added "Dettagli" link in actions column)
  - prd.json (US-031 passes: true)
- **Learnings for future iterations:**
  - The InvoicePDF template already supports type="quote" (shows expiryDate) and type="creditNote" (shows referenceInvoice) - no template changes needed
  - Quote uses EUR as default currency since the Quote model doesn't store currency (unlike Invoice)
  - Credit note inherits currency and disclaimer from its parent invoice record
  - A reusable DownloadPDFButton component (src/components/DownloadPDFButton.tsx) can serve all document types via the documentType prop
  - Quote detail page needed to be created since it didn't exist (unlike invoice and credit note which had existing detail pages)
---

## 2026-01-22 - US-032
- What was implemented: Authentication system with login page at /login (email/password form), bcryptjs password hashing, JWT session management via httpOnly cookie using jose library, middleware protecting all routes except /login, logout button in sidebar, and initial user seeding from environment variables (ADMIN_EMAIL, ADMIN_PASSWORD). Restructured app to use (main) route group for authenticated pages with sidebar layout, keeping /login at the root level without sidebar.
- Files changed:
  - prisma/schema.prisma (added User model with email unique, password, timestamps)
  - prisma/migrations/20260122100000_add_user_model/migration.sql (User table creation)
  - prisma/seed.ts (added admin user seeding with bcrypt hashed password from env vars)
  - src/lib/auth.ts (new - JWT token creation/verification with jose, password hashing, cookie management)
  - src/middleware.ts (new - route protection checking auth-token cookie, redirects to /login)
  - src/app/login/page.tsx (new - login page)
  - src/app/login/LoginForm.tsx (new - client form component with email/password, error handling)
  - src/app/login/actions.ts (new - login server action with Zod validation, user lookup, password verify, token creation)
  - src/app/(main)/layout.tsx (new - authenticated layout with Sidebar)
  - src/app/(main)/logout/actions.ts (new - logout server action removing cookie and redirecting)
  - src/app/layout.tsx (updated - removed Sidebar, now just html/body shell)
  - src/components/Sidebar.tsx (updated - added logout button at bottom)
  - src/app/(main)/* (moved all app pages into route group)
  - package.json, package-lock.json (added bcryptjs, jose, @types/bcryptjs)
  - .env.example (added JWT_SECRET, ADMIN_EMAIL, ADMIN_PASSWORD)
  - prd.json (US-032 passes: true)
- **Learnings for future iterations:**
  - Route groups like (main) are transparent in URL routing but NOT in TypeScript imports - use `@/app/(main)/...` for cross-file imports
  - Moving pages into a route group requires updating all cross-module imports that reference those paths
  - jose library works in both Node.js and Edge runtime (unlike jsonwebtoken which is Node-only)
  - Next.js middleware.ts at src/ root catches all requests - use matcher config to exclude static files
  - Login page should NOT be in the (main) group since it needs its own layout (no sidebar)
  - Logout as server action with form element allows native form submission without JavaScript
  - bcryptjs is the pure JS implementation of bcrypt - no native compilation needed for Docker
  - The `cookies()` API in Next.js 16 returns a Promise that must be awaited
---

## 2026-01-22 - US-033
- What was implemented: Change password form in the settings page (/impostazioni) with current password verification, new password with confirmation field, minimum 8 character validation, and success/error feedback. Uses Zod schema with refinement for password match, getCurrentUser for auth context, and verifyPassword/hashPassword from auth utilities.
- Files changed:
  - src/app/(main)/impostazioni/actions.ts (added changePassword server action with Zod validation, current password check, and password update)
  - src/app/(main)/impostazioni/ChangePasswordForm.tsx (new - client component form with password fields and feedback)
  - src/app/(main)/impostazioni/page.tsx (updated - added ChangePasswordForm section below company data)
  - prd.json (US-033 passes: true)
- **Learnings for future iterations:**
  - Zod .refine() can be used on object schemas for cross-field validation (e.g., password confirmation matching)
  - getCurrentUser() from auth.ts returns userId/email from the JWT token - useful for identifying the current user in server actions
  - Change password pattern: verify current password → hash new password → update user record
  - Settings page can stack multiple card sections vertically with mb-6 spacing between them
  - Form resets fields to empty on successful password change for security (no lingering password data in state)
---
